import Foundation
import Core

// Capability surface lives on MercuryBrain (`invoke`, `retrieveSnapshot`).
// This file retained only as a marker so older imports do not break paths.
// Safe to delete once no external references remain.
