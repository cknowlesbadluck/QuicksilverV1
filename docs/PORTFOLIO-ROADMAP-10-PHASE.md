# Portfolio 10-phase roadmap — 2026-09-25 17:00 EDT

Owner-blocked items stay owner-blocked. Agents do not invent Netlify secrets or claim device HG.

1. **Ready-or-refuse on main** — DONE (#108). Live still 503 until SERVICE_ROLE is set.
2. **Owner: set resonancenexus env** — `SUPABASE_SERVICE_ROLE_KEY` on the existing site only. Exit: `GET /api/ready` 200.
3. **Prove durable GitHub vertical slice** — member-only `github.repository.read` evidence that survives restart; other-project and anonymous callers are denied.
4. **Device HG CHR-55** — Archive IPA **on iPhone 16e**. GitHub Actions success on `38f7b0fb` is not acceptance.
5. **Quicksilver next code slice after HG** — CHR-12 realm a11y, or first on-device AI turn after Covenant bind. Land #152 only as telemetry hygiene, not as a substitute for HG.
6. **Conduit freeze + repair red drafts** — keep #119/#120 draft until verify+postgres green on current main.
7. **Conduit #125 keyset pagination** — merge after rebase if required checks stay green. Workers Builds does not apply to #125 (keyset pagination / API only; no Workers runtime files).
8. **Resonance iOS I1** — compose → plan → approve → execute → evidence against a ready host. Blocked by phase 2.
9. **Chamber lifecycle slice** — form / work / dissolve with audit intact.
10. **Release surface** — SideStore/IPA evidence, Privacy Manifest, production-smoke unskipped, branch entropy cut by owner (no delete-ref tool here).
