# Quicksilver Recovery Plan — 2026-08-11

**Mode:** Aggressive yet methodical  
**Owner:** Forge (Grok coordination)  
**Branch:** `recovery/aggressive-stabilization-2026-08-11`  
**Base:** `main` @ `6b017ec`

## Decision

We adopt the **additive recovery foundation** as the first concrete step.

- Pure additive Core types: MercuryIdentity, MercuryExpression, MercuryAspectResolver, MercuryCapabilities (Relic/Glyph), CapabilityBroker, MercuryProviderRouter.
- Does **not** switch live paths. PersonaManager and current MercuryBrain remain the production path until a later, deliberate migration PR.
- Parallel work (Brain+Memory fusion from #55, Integration Fabric from #66, Living Realms) will be rebased or cherry-picked onto this foundation after it lands green.

## Sequence (do not skip)

1. **Land pure additive Core** ← **IN PROGRESS**  
   Clean foundation committed directly on this recovery branch (avoided the incidental UI/lint noise from PR #63 that kept CI red). Await green CI, then merge to main.

2. **Stabilize main**  
   - Merge or close #62 (docs/architecture contracts).  
   - Close or retarget obviously superseded draft PRs.  
   - Confirm Structure, Lint, SPM tests, Simulator Build are green.

3. **Intelligence integration**  
   Bring the clean parts of #55 (context-aware Brain path, memory ranking, Nexus signals) onto the new foundation without breaking the additive boundary.

4. **Living Realms**  
   Drive Forge / Eternal / Codex functionality (CHR-10, CHR-11) against the single baseline. No new architecture forks.

5. **Visual foundation**  
   CHR-16 (Figma) → CHR-18 (SwiftUI) → CHR-17/19 reviews. Treat as a gate, not a side quest.

6. **Release hygiene**  
   Tag after the above is coherent. Prune branches aggressively.

## Invariants (non-negotiable)

- Sense → Think → Express direction.  
- Core owns protocols and shared models.  
- MercuryBrain remains the only intelligence surface for UI/Intents.  
- Nexus stays persona-agnostic.  
- Public Apple APIs only. SideStore-first.  
- No large unilateral rewrites. Small, reviewable steps with green CI between them.

## Status Log

- 2026-08-11 (early): Audit complete. Recovery branch created. Linear CHR-5 + CHR-14 updated.
- 2026-08-11 (later): Clean additive foundation committed on recovery branch:
  - `Core/MercuryIdentity.swift`
  - `Core/MercuryAspectResolver.swift`
  - `Core/MercuryCapabilities.swift`
  - `Core/CapabilityBroker.swift`
  - `Core/MercuryProviderRouter.swift`
  - `Tests/MercuryArchitectureTests.swift`
  PR #63 left in place but no longer the delivery vehicle (it carried incidental UI renames + lint rule changes that kept CI red).
- 2026-08-12: Stabilization pass (Forge). Made MercuryProviderPolicy defaults provider-neutral (`primary` / `secondary` / `fallback` IDs). Expanded MercuryArchitectureTests for authorization policy, missing glyph, and provider router fallback paths. No live-path changes. Pre-existing SwiftLint failures on UI (e.g. AmbientLayer identifier_name) remain out of scope for this additive PR.
