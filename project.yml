name: Quicksilver
options:
  bundleIdPrefix: com.quicksilver
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true
  groupSortPosition: top
  developmentLanguage: en

settings:
  base:
    SWIFT_VERSION: "6.0"
    TARGETED_DEVICE_FAMILY: "1"
    IPHONEOS_DEPLOYMENT_TARGET: "18.0"
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    SWIFT_STRICT_CONCURRENCY: complete

packages:
  Sentry:
    url: https://github.com/getsentry/sentry-cocoa
    from: 9.25.0

targets:

  # ---- Modules (match Package.swift) ----

  Core:
    type: framework
    platform: iOS
    sources:
      - path: Core
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quicksilver.core
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGNING_ALLOWED: NO

  Memory:
    type: framework
    platform: iOS
    sources:
      - path: Memory
    dependencies:
      - target: Core
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quicksilver.memory
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGNING_ALLOWED: NO

  Personas:
    type: framework
    platform: iOS
    sources:
      - path: Personas
    dependencies:
      - target: Core
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quicksilver.personas
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGNING_ALLOWED: NO

  ServicesAI:
    type: framework
    platform: iOS
    sources:
      - path: Services/AI
    dependencies:
      - target: Core
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quicksilver.servicesai
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGNING_ALLOWED: NO

  Nexus:
    type: framework
    platform: iOS
    sources:
      - path: Nexus
        excludes:
          - "*.md"
    dependencies:
      - target: Core
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quicksilver.nexus
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGNING_ALLOWED: NO

  QuicksilverIntents:
    type: framework
    platform: iOS
    sources:
      - path: Intents
    dependencies:
      - target: Core
      - target: Personas
      - target: Nexus
      - target: Memory
      - target: ServicesAI
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quicksilver.intents
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGNING_ALLOWED: NO

  # ---- App ----

  Quicksilver:
    type: application
    platform: iOS
    sources:
      - path: App
      - path: UI
      - path: Models
        optional: true
    resources:
      - path: Resources          # Persona prompts must ship in the IPA
      - path: PrivacyInfo.xcprivacy
    dependencies:
      - target: Core
      - target: Memory
      - target: Personas
      - target: ServicesAI
      - target: Nexus
      - target: QuicksilverIntents
      - package: Sentry
        product: SentrySPM
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quicksilver.app
        PRODUCT_NAME: Quicksilver
        MARKETING_VERSION: "0.2.0"
        CURRENT_PROJECT_VERSION: "7"
        INFOPLIST_KEY_CFBundleDisplayName: Mercury
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait
        INFOPLIST_KEY_LSRequiresIPhoneOS: YES
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_STYLE: Automatic
        # SideStore-friendly: no private entitlements, public APIs only.
        # Build floor = iOS 18.0 (current CI SDK). Primary validation device = iOS 27.
      configs:
        Release:
          DEBUG_INFORMATION_FORMAT: dwarf-with-dsym
          GCC_GENERATE_DEBUGGING_SYMBOLS: YES

  QuicksilverTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests
    dependencies:
      - target: Quicksilver
      - target: Core
      - target: Memory
      - target: Personas
      - target: ServicesAI
      - target: Nexus
      - target: QuicksilverIntents
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES

schemes:
  Quicksilver:
    build:
      targets:
        Core: all
        Memory: all
        Personas: all
        ServicesAI: all
        Nexus: all
        QuicksilverIntents: all
        Quicksilver: all
        QuicksilverTests: [test]
    test:
      targets:
        - QuicksilverTests
    run:
      config: Debug
    archive:
      config: Release
